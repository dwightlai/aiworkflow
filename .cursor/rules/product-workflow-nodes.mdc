---
description: 工作流节点设计原则——产品化，禁止项目专用节点
alwaysApply: true
---

# 工作流节点产品化原则

本产品是通用 AI 工作流平台，不是某个具体业务项目的定制系统。

## 禁止

- 为单一业务场景（如编研、档案馆、合同审查）新增 `WorkflowNodeType` 或专用节点执行器
- 在工作流引擎中硬编码领域实体加载逻辑（如 `GENERATION_TEMPLATE`、`ARCHIVE_CORPUS`）

## 应使用

- **HTTP_TOOL**：调用 REST API 获取外部/内部数据
- **CONTENT_TEMPLATE / TEXT_TRANSFORM**：变量渲染与文本组装
- **KNOWLEDGE_RETRIEVAL / LLM / LOOP / CONDITION** 等已有通用节点

## 业务数据接入方式

1. 业务模块暴露标准 REST API（如 `/api/generation-templates/{id}/runtime`）
2. 工作流通过 HTTP 节点调用 API，用路径表达式引用响应字段（如 `templateHttp.body.data.sections`）
3. 领域逻辑留在 Service/API 层，不进入工作流引擎

## 示例

```
❌ 新增 ARCHIVE_CORPUS 节点执行器
✅ HTTP GET /api/research/mock/theme-libraries/{{themeLibraryId}}/corpus
```
